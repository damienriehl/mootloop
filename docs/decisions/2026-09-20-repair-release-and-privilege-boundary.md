# Repair release and privilege completion boundary

Recorded: 2026-09-20, from the owner's explicit session response.

## Authorized release

The owner authorized preparation and full local validation of a repair-only branch,
then opening a PR, lifting the session's network/push restrictions, merging, and
deploying. This supersedes the earlier session prohibition on those actions for
this repair release. U-13 and U-12 scaffolding stay separate; D-10 A remains the
settled policy for subsequent feature expansion.

Existing confidentiality boundaries remain: no real-matter reads/runs, credential
contents, or substantive attorney approvals are authorized by this release decision.
Use synthetic fixtures for tests and preserve unrelated dirty files. Deploy existing
core web/API and synthetic demo services through their configured deployment path;
per-matter workers retain their separate drain and protected-operation requirements.

## Privilege workflow

The owner accepted the recommendation that clean export should require a
**reviewed package ready for service**, with **actual delivery tracked separately**.
This settles the future product boundary; it does not mean a current package has
been reviewed or delivered.

The present repair retains the conservative privilege export gate. A later scoped
implementation must bind human review to the exact response, document dispositions,
and privilege-log state before it can establish readiness. Schema design, partial
production/redaction semantics, and attorney review criteria still need that design
work; this decision alone does not approve any substantive privilege disposition.

Sources: the owner's three affirmative answers in the 2026-09-20 session and the
integration review that distinguished repaired behavior from gated feature scope.
