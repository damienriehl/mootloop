---
artifact_contract: "ce-handoff/v1"
created_at: "2026-08-06T16:53:12Z"
title: "mootloop Aug 5 hardening and open-PR orientation"
summary: "Verified the active hardening branch, two open green PRs, local full gate, and timer-managed worktree state."
keywords: ["mootloop", "security-hardening", "anti-hedge", "reopen-verb", "pull-request"]
cwd: "/home/damienriehl/Coding Projects/mootloop"
resume_focus: "Review and land the two clean open PRs deliberately, then reconcile the branch's broader hardening series."
repository: "mootloop"
repo_root_sha: "8b125150bd30a286e1967602afdb4a7a4392020a"
branch: "fix/associate-anti-hedge-nudge"
head: "8e6bfdcf65210e2511e42c9d645e422f57198f03"
worktree_path: "/home/damienriehl/Coding Projects/mootloop"
---

# mootloop Aug 5 hardening and open-PR orientation

## Current state

- Active branch `fix/associate-anti-hedge-nudge` is exactly at its remote, HEAD `8e6bfdc`.
- The only local modification at capture is `.claude/RESUME.md`, auto-regenerated by
  `rc-wip.timer`. Preserve it as user/tool-managed state; do not fold it into unrelated work.
- The newest prior repo handoff was dated 2026-07-15, so it predates this hardening series.

## Open pull requests (verified from GitHub on 2026-08-06)

- PR #30, `fix/associate-anti-hedge-nudge` -> `main`: “hard anti-hedge rule in the
  associate body (Liguria Foods).” It is open, non-draft, cleanly mergeable, and all four
  reported checks are successful. The local checkout is this PR's head.
- PR #31, `feat/reopen-verb` -> `main`: “first-class reopen verb for needs_attention
  runs.” It is open, non-draft, cleanly mergeable, and all four reported checks are
  successful. Its checks last ran July 24; revalidate against the current base before merge.
- These are independent heads. Do not infer a merge order or merge either merely because
  CI is green; current user authority governs external mutations.

## What landed on the active branch

The Aug 5 commits extend well beyond the anti-hedge title. They include fixes for the
persona sandbox and denied-tool handling, prompt exposure on argv, run-lock heartbeat,
fact-revision crash consistency, privacy scanning of symlinks, process-wide citation rate
limiting, interruptible shutdown, journal performance, model-cost accounting, gate behavior,
and several earlier security/correctness findings. Start with the commit log from `016ea84`
through `8e6bfdc` and `docs/audits/2026-08-05-blind-persona-turn-audit.md` rather than the
stale July handoff.

The cockpit On Deck entry says the Aug 5 three-question security review was answered and
executing, and records remaining product questions around cured fabrication findings and
web exposure of `superseded`. Treat that text as queue context, not proof of completion;
verify code and current ask state before acting.

## Verification on this box

- 2026-08-06 `make check` passed completely: ruff clean, strict mypy clean across 82 source
  files, and `645 passed, 1 warning in 124.29s`, with 92% total coverage.
- The sole warning is Starlette's deprecation notice for FastAPI TestClient's current httpx
  integration; it is not a test failure.
- The first sandboxed attempt failed before tests because uv's cache was mounted read-only
  and process streams were denied. The successful count above is from the direct host run.

## Operational gotchas and constraints

- Codex creates an empty `/tmp/.git` mountpoint. Commit `689ce4c` correctly changed vault
  repo detection so an empty directory is not treated as a repository; `.git/HEAD` or a
  gitlink file is required. Do not “fix” the safety guard back to `.exists()`.
- Preserve matter-vault confidentiality and all fail-closed boundaries in `AGENTS.md`.
- Never inspect `*.pem`, `*.key`, `twin-secrets/`, or `~/.secrets`.
- Never enter or touch `fence-litigation/` or `bayless-aerials/`.
