# MootLoop — RESUME (single source of truth for a fresh context)

**Last updated 2026-07-15.** This supersedes earlier handoffs. Read this first; it is
self-contained. Cross-checks: `README.md` (§ Hosted tier), `docs/security-frontend.md`
(threat model), `docs/deploy-matter.md` (matter runbook), `docs/backup.md` (backup/restore),
`docs/evidence/fe-0-pen-gate.html` (pen-gate attestation), `docs/plans/2026-07-12-001-*`
(the FE plan; FD-1..FD-10 are the amendments).

---

## TL;DR — where the project is

The hosted **matter tier** is LIVE and healthy at **https://mootloop.damienriehl.com**,
behind Cloudflare Access with a **per-hostname AOP origin-lock** (only Cloudflare can reach
the origin). The **FE-0 penetration gate is GREEN** (all 13 assertions hold). **Both FD-6
data-lifecycle gates are DONE** (`mootloop close` + encrypted backup/restore). PRs #15–23 are
merged to `main`. The public **demo** tiers are also live (dev + prod apex).

> **SESSION ADDENDUM — 2026-07-15 (post-token).** The engine token is now installed AND
> **proven live**: a `claude -p` probe inside the driver container returned `PONG` (exit 0)
> on Damien's Max token — the FE-7 unlock is confirmed. Two gaps surfaced, neither a
> regression:
> 1. **Hosted matter-create hit a read-only mount** — `mootloop init` seeds `canaries.json`
>    into `~/.mootloop` (mounted `:ro`) → `OSError [Errno 30]`. **Fixed in PR #25**
>    (`fix/hosted-canary-registry-ro-mount`): canary registry honors a
>    `MOOTLOOP_CANARY_REGISTRY` override, pointed at a writable matters-root path for
>    api+driver; local dev unchanged. `make check` 503✓. **Needs a `mootloop-matter`
>    redeploy to take effect.**
> 2. **No usable path to move the fence documents onto the box** — rclone/gdown/gcloud are
>    absent on both machines; the Drive folder is private (no anon download); the Google
>    Drive MCP returns each file as base64 *into context* (a 300 KB PDF ≈ hundreds of K
>    tokens; corpus ≈300+ files / several hundred MB). **This is the one true remaining
>    blocker — needs Damien's direction** (recommend: he drops the Drive folder as a zip
>    onto a box staging dir; I ingest → backup → run). Matter inventory characterized
>    (counts/sizes/types only) in the readiness artifact.
>
> **Readiness + decision artifact:** https://claude.ai/code/artifact/ce3153b5-db98-4395-8edb-224ffddba246
> (round-trippable — captures Decision A transfer path, B matter identity, C when to ship PR #25).
> **Box pipeline proven** on synthetic data: `init→validate→ingest→backup` (encrypted
> `*.tar.gz.enc`) all pass; scratch vault purged.

**The only things between here and a first live hosted run (FE-7):**
1. ~~A valid **engine token** on the box~~ — **DONE + proven (`claude -p`→PONG).**
2. **Decide + execute the document transfer** (Decision A), then **seed the fence matter**,
   `mootloop backup`, and kick the FE-7 run. Merge+redeploy PR #25 as part of this (Decision C).

Everything else needed is already in place (incl. `MOOTLOOP_BACKUP_KEY`).

**Verified live 2026-07-15:** `mootloop.damienriehl.com/` → 302 (Access); `mootloop.org/health`
→ 200; all three matter-tier containers healthy (~47h uptime); box secrets present:
`MOOTLOOP_INTERNAL_SECRET`, `MOOTLOOP_DOWNLOAD_SIGNING_KEY`, `MOOTLOOP_BACKUP_KEY`,
`CLAUDE_CODE_OAUTH_TOKEN` (prefix-count 1, engine-verified).

---

## Infrastructure map (all IDs, paths, hosts)

### Hosts & access
- **Dev box (hetzner-dev / riehl-dev):** `204.168.246.227`. SSH `root@204.168.246.227`
  (BatchMode key auth works). Also the demo host.
- **This dev machine (Damien's home Linux box):** public IP `97.116.181.129`. Where Claude
  Code + these sessions run. `~/.mootloop/secrets.env` here holds `CLOUDFLARE_MOOTLOOP_TOKEN`
  + `CF_ACCESS_AUD` + a comment with the **fence-litigation Google Drive folder link**.

### Cloudflare
- Account `05b5e8e80e510bd7cf1f08cb78ae9d07`; zone **damienriehl.com** `45539317ebc2598f913b867756fa58ea`;
  Zero Trust team `young-unit-68fd.cloudflareaccess.com`.
- Token: `~/.mootloop/secrets.env` → `CLOUDFLARE_MOOTLOOP_TOKEN`. Scopes now include DNS edit,
  Access apps edit, **and SSL/Certificates edit** (Damien added the last on 2026-07-13). Still
  lacks account Zone→Create.
- **Access apps:** "MootLoop Matter Tier" on `mootloop.damienriehl.com` — AUD
  `0930916199c5086d48182201764ae6bb45f6f68be1e6568ab82ebc798705a177` (stored as `CF_ACCESS_AUD`),
  allow policy email `damienriehl@gmail.com`, 24h session. Plus an **ACME-challenge bypass** app
  on `mootloop.damienriehl.com/.well-known/acme-challenge/` (lets Let's Encrypt validate behind
  Access — do not delete).
- **DNS:** proxied `A mootloop.damienriehl.com → 204.168.246.227`.
- **AOP (per-hostname Authenticated Origin Pulls) — LIVE:** client cert uploaded to the zone's
  per-hostname store (cert_id `73d666f6-4d78-4559-a13d-15478b3dfb50`, binding active). CF presents
  it on origin pulls; the origin requires+verifies it. **Only Cloudflare reaches the origin.**

### The box — AOP + Traefik
- CA + client cert: `/root/mootloop-aop/{aop-ca.crt,aop-client.crt,aop-client.key}`.
- Traefik (v3.6, container `coolify-proxy`, mounts `/data/coolify/proxy → /traefik`): the AOP
  option is `/data/coolify/proxy/dynamic/mootloop-aop.yaml` (`RequireAndVerifyClientCert`, caFiles
  = `/traefik/certs-aop/mootloop-aop-ca.crt`). The **web router references it via a compose label**
  (`traefik.http.routers.https-0-op3exbmxe1q21qyy69tlngk9-web.tls.options=mootloop-aop@file`) — see
  `docker-compose.matter.yaml`. Router name is derived from the app UUID + service; **re-derive if
  the app is ever recreated.**
- ⚠️ **Do NOT set `RequireAndVerifyClientCert` on any router before CF is confirmed presenting the
  cert for that host** — it would break the live site. (Already safely live here.)

### Coolify (on the box)
- API at `http://localhost:8000` (from the box). Token file: `/home/deploy/.coolify-token`
  (NOT root's home). **This token was rotated 2026-07-13** (the original expired mid-session); the
  current one is read-write and works. If deploys start 401ing, the token expired again — Damien
  regenerates at `https://coolify.dev.openlegalstandard.org/security/api-tokens` and I rewrite
  `/home/deploy/.coolify-token`.
- Server UUID `azqkiidl028fi9yqbf7wg7nc`. Project **mootloop** `p5hcn07bhqx0h8eb0vhyia94`
  (env `production`, uuid `dymnn2w4xtu2sooauqu8avp3`).
- **Apps in the project:**
  - `mootloop-matter` (compose app) uuid **`op3exbmxe1q21qyy69tlngk9`** — the matter tier. Deploys
    from `main`, `docker-compose.matter.yaml`. Containers: `web-op3exbmxe…` (BFF, public),
    `api-op3exbmxe…` (internal, unpublished), `driver-op3exbmxe…` (worker). Env vars set in Coolify:
    `MOOTLOOP_INTERNAL_SECRET`, `CF_ACCESS_TEAM_DOMAIN`, `CF_ACCESS_AUD`, `CF_ACCESS_ALLOWED_EMAIL`,
    `MOOTLOOP_WORKER_ID`. Deploy: `POST /api/v1/deploy?uuid=op3exbmxe1q21qyy69tlngk9&force=false`.
  - `mootloop-prod` uuid **`wx0ow6y0tfwnlxb5tupxg2dl`** — public demo PROD (mootloop.org). fqdn now
    `https://mootloop.org,https://www.mootloop.org` BUT only apex has a cert; **www needs a
    demo-prod redeploy** to issue its cert (deferred by Damien — prod deploys are ask-gated).
  - demo DEV app (`mootloop.dev.openlegalstandard.org`) — older, auto-deploys via GitHub webhook.
- **House recipe:** serial builds only (`concurrent_builds=1` — concurrent builds OOM the box);
  `docker system prune -af` before builds when disk is tight (was 87%→70% after a prune 2026-07-12).

### The box — matter service user & vault
- Service user **`mootloop`, UID/GID 3200** (the matter-api + driver images bake this exact uid so
  the bind-mounted vault + secrets own correctly). HOME `/home/mootloop`.
- Matters-root **`/srv/mootloop-matters`** (0700, owned mootloop:mootloop). Vaults live here,
  outside every repo.
- Secrets **`/home/mootloop/.mootloop/secrets.env`** (0600). Present: `MOOTLOOP_INTERNAL_SECRET`,
  `MOOTLOOP_DOWNLOAD_SIGNING_KEY`, `MOOTLOOP_BACKUP_KEY`. **Missing: `CLAUDE_CODE_OAUTH_TOKEN`**
  (the engine token — the one remaining blocker). The api/driver containers mount `~/.mootloop`
  **read-only**, so any key that self-derives on first use must be pre-seeded (that's why the
  signing + backup keys were seeded by hand).

### Namecheap (mootloop.org registrar/DNS)
- Creds at `~/.secrets/namecheap` (0600): `NAMECHEAP_API_USER=damienriehl`, `NAMECHEAP_API_KEY=…`.
  IP allowlist includes `204.168.246.227` + `97.116.181.129`. API base `https://api.namecheap.com/xml.response`.
- ⚠️ **`setHosts` REPLACES all host records** — always `getHosts` first, then set the full list.
- mootloop.org A records `@` + `www` → `204.168.246.227` are set (apex live). ⚠️ **The key was
  pasted in chat on 2026-07-13 — Damien should rotate it; update `~/.secrets/namecheap` after.**

---

## What is DONE (merged to main)

- **PRs #1–14** (prior sessions): the whole pipeline + demo tier + Next.js frontend
  (FE-0 perimeter code, FE-1 sandboxed engine, FE-2 cockpit+inbox, FE-2.5 freeform on-ramp+export).
- **PR #15** — matter-tier deploy infra: `Dockerfile.matter-api`, `Dockerfile.driver`
  (claude-code pinned `2.1.207`, bubblewrap egress jail, uid 3200), `docker-compose.matter.yaml`,
  unauthenticated `/health`, `docs/deploy-matter.md`.
- **PR #16** — auth-before-resolve fix: per-matter routes resolved the vault before checking auth,
  leaking an unauthenticated 400/404 matter-ID oracle. Reordered auth ahead of `resolve_matter` on
  all 19 routes + a no-oracle regression test. (The pen gate caught this live.)
- **PR #17 / #20** — pen-gate attestation (`docs/evidence/fe-0-pen-gate.html`) + perimeter-green update.
- **PR #19** — attached the AOP `tls.options` to the web router (closed pen item 1).
- **PR #21** — FD-6 **close-inventory** gate: `src/mootloop/close.py` (`MATTER_SCOPED_STORES`
  inventory), `tests/invariants/test_close_inventory.py` (CI invariant — fails if any concrete
  `VersionedModel` is neither registered nor exempt), `mootloop close` (idle-only, backup-first,
  containment-safe purge via `safe_vault_path`, post-purge residue assert, anonymized hash-chained
  `AccessAuditEntry` tombstone + `CloseRecord` in `matters-root/.closed/`). `CloseError`,
  `MatterProvenanced` mixin, `CloseRecord` model.
- **PR #22** — FD-6 **backup** gate: AES-256-GCM encryption-at-rest (`<matter>-<ts>.tar.gz.enc`,
  key `MOOTLOOP_BACKUP_KEY` via `load_or_create_backup_key`), traversal-safe `restore_matter` +
  `mootloop restore`, restore drill (round-trip + wrong-key/tamper/truncation/malicious-tar all
  fail-closed). `docs/backup.md`. RPO = last snapshot; off-box push documented (rclone/rsync).
- **PR #23** — docs: FD-6 done.
- **Live infra actions (not code):** DNS, Access apps, AOP cert upload+enable, Coolify app create +
  env + deploys, mootloop.org Namecheap DNS, `MOOTLOOP_BACKUP_KEY` seeded on box.

`make check` on `main`: **501 passed, 2 skipped** (mypy --strict + ruff + pytest). Green.

---

## What REMAINS — exact steps

### 1. Engine token (the one blocker for any live run)
Damien runs `claude setup-token` (any device with the Claude CLI + his Max login) and provides the
value — it **must start `sk-ant-oat01-`** (on 2026-07-13 he pasted a non-`sk-ant-oat` value; it was
NOT written — always verify the prefix). Then place it on the box:
```bash
# with TOK = the sk-ant-oat01-… value (never echo it; register for redaction)
ssh root@204.168.246.227 'read -r TOK
  umask 077
  # append or replace CLAUDE_CODE_OAUTH_TOKEN in the mootloop secrets file
  grep -v "^CLAUDE_CODE_OAUTH_TOKEN=" /home/mootloop/.mootloop/secrets.env > /tmp/s.$$ || true
  printf "CLAUDE_CODE_OAUTH_TOKEN=%s\n" "$TOK" >> /tmp/s.$$
  install -m 600 -o mootloop -g mootloop /tmp/s.$$ /home/mootloop/.mootloop/secrets.env
  rm -f /tmp/s.$$' <<< "$TOK"
```
The driver reads it via the read-only `~/.mootloop` mount (no redeploy needed; the driver picks it
up on its next turn — or restart the driver container to be certain).
⚠️ Reading Claude OAuth creds on this machine is blocked by `~/bin/dev-twin-bash-guard` (a local
safety hook) — I cannot lift the existing local token myself; the value must come from Damien.

### 2. Seed the fence matter
Source is the **Google Drive folder** (link in `~/.mootloop/secrets.env` as a comment). Pull via
Drive MCP or rclone → rsync into a new vault under `/srv/mootloop-matters/<matter_id>/` on the box
(matter_id must match `^[a-z0-9][a-z0-9._-]{0,63}$`, opaque, never party-derived). Create the matter
via `mootloop` CLI or the API. **Matter data NEVER goes in the repo.** Take a first encrypted backup
(`mootloop backup`) once seeded.

### 3. First live run (FE-7)
Kick off from the web cockpit or CLI; the driver runs `claude -p` persona turns server-side. Watch
the spend ledger + gate ledger. This is the milestone the whole tier exists for.

---

## Load-bearing gotchas (don't relearn these the hard way)
- **AGENTS.md is law:** mypy --strict is the authoritative gate; `make check` green before every
  commit; matter data never in repo; all vault writes through `safe_vault_path`; fail closed.
- **Model roles:** Fable orchestrates/plans/reviews; **Opus subagents do execution work**
  (`Agent` tool `model: "opus"`). This session's build agents were Opus.
- **Branch → PR → CI green → review → merge.** Never commit straight to main. Commit trailer:
  `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` + `Claude-Session: <url>`. PR body ends
  with the 🤖 Generated-with footer + session URL.
- **Prod deploys are ask-gated** (house rule) — confirm before deploying `mootloop-prod` (demo).
  Matter-tier (`mootloop-matter`) deploys are fine autonomously.
- **Coolify token can expire** — if API 401s, regenerate + rewrite `/home/deploy/.coolify-token`.
- **Let's Encrypt rate-limits** 5 failed authz/hour/domain — don't spam ACME; point DNS first.
- **Namecheap `setHosts` replaces everything** — getHosts first.
- **Artifacts (review deliverables):** interactive HTML with an `execCommand` clipboard fallback
  (`navigator.clipboard` is blocked in the artifact iframe). Theme-aware light+dark. The pen-gate
  attestation + two action sheets used a "courtroom-ledger" identity (serif + oxblood/ochre/sealed).
- **Deferred per FD-10 (do NOT build before the first live run):** FE-3 wizard/catalog + task
  synthesis + failover UX; FE-4 strategy board (React Flow); FE-5 upload UI + Drive watcher +
  deadline scheduler; FE-6 dashboard/digest/mute; Dropbox/OneDrive; Web Push; pipeline_shape
  registry; multiplayer. Known small **FE-3 nit**: freeform keyword resolver singular/plural
  mismatch with the omnibox placeholder — fold into the FE-3 batch.
- **Damien's other reminders:** CourtListener token (live citation gate); Google OAuth →
  "In Production" (FE-5 Drive watcher only); rotate the Namecheap key; www.mootloop.org cert
  (needs a demo-prod redeploy — deferred).

---

## Suggested first action in a fresh context
Run the TL;DR verification block (perimeter curls + container check + box secrets `cut -d= -f1`).
If `CLAUDE_CODE_OAUTH_TOKEN` is still absent, the state is exactly as this doc describes and the
next move is Damien's engine token + the fence seed. If present, a live run may have started —
check `/srv/mootloop-matters` and the driver logs.
